len=54 | 'shipment_id → shipment.id; actor_user_id → app_user.id'  ||  H7 (ERD: T16) — shipment_status_history
len=78 | 'customer_id → customer.id; currency_id → currency.id; created_by → app_user.id'  ||  T17 — invoice
len=52 | 'invoice_id → invoice.id; actor_user_id → app_user.id'  ||  H4 (ERD id) — invoice_history
len=44 | 'invoice_id → invoice.id; order_id → order.id'  ||  J1 — invoice_order (Junction)
len=79 | 'customer_id → customer.id; currency_id → currency.id; received_by → app_user.id'  ||  T19 — payment
len=48 | 'payment_id → payment.id; invoice_id → invoice.id'  ||  J2 — payment_allocation (Junction)
len=77 | 'credit_note_id → credit_note.id; invoice_line_id → invoice_line.id (nullable)'  ||  T21 — credit_note_line
len=31 | 'customer_id → customer.id (1:1)'  ||  M13 — customer_ledger (Projection — Non-Authoritative)
len=70 | 'bot_session_id → bot_session.id; bot_platform_id → bot_platform_ref.id'  ||  H5 (ERD id) — bot_message_log
len=66 | 'actor_user_id → app_user.id; entity_type + entity_id (polymorphic)'  ||  H6 (ERD id) — audit_log
len=70 | 'approval_request_id → approval_request.id; actor_user_id → app_user.id'  ||  H7 (ERD id) — approval_history
len=66 | 'stock_transfer_id → stock_transfer.id; actor_user_id → app_user.id'  ||  H4 (ERD: T6) — transfer_history *(ERD numbering: T6; classified H — immutable, listed under its ERD id for traceability.)*
len=48 | 'order_id → order.id; actor_user_id → app_user.id'  ||  H5 (ERD: T12) — order_status_history
len=66 | 'order_line_id → order_line.id; price_history_id → price_history.id'  ||  H6 (ERD: T13) — order_price_freeze (optional)
len=64 | 'entity_type + entity_id (polymorphic); uploaded_by → app_user.id'  ||  M14 — attachment
len=54 | 'product_id → product.id; attachment_id → attachment.id'  ||  M15 — product_image
len=64 | 'report_type_id → report_type_ref.id; owner_user_id → app_user.id'  ||  M17 — report_definition
len=73 | 'notification_id → notification.id; actor_user_id → app_user.id (nullable)'  ||  H8 (ERD id) — notification_history
len=74 | 'report_run_id → report_run.id; report_definition_id → report_definition.id'  ||  H9 (ERD id) — report_snapshot