# Backend API base URL. When using the Vite proxy (server.proxy in vite.config.ts),
# leave this empty — requests to /api/* are proxied to http://localhost:8000.
# For production or a remote backend, set the full URL:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
